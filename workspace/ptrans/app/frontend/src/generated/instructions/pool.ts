/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit'
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core'
import { findNullifierSetPda, findPoolPda, findPoolVaultPda } from '../pdas'
import { PTRANS_PROGRAM_ADDRESS } from '../programs'

export const POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  134, 215, 119, 168, 28, 199, 193, 127,
])

export function getPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POOL_DISCRIMINATOR)
}

export type PoolInstruction<
  TProgram extends string = typeof PTRANS_PROGRAM_ADDRESS,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountNullifierSet extends string | AccountMeta<string> = string,
  TAccountPoolVault extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPool extends string ? WritableAccount<TAccountPool> : TAccountPool,
      TAccountNullifierSet extends string
        ? WritableAccount<TAccountNullifierSet>
        : TAccountNullifierSet,
      TAccountPoolVault extends string ? WritableAccount<TAccountPoolVault> : TAccountPoolVault,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type PoolInstructionData = { discriminator: ReadonlyUint8Array }

export type PoolInstructionDataArgs = {}

export function getPoolInstructionDataEncoder(): FixedSizeEncoder<PoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: POOL_DISCRIMINATOR }),
  )
}

export function getPoolInstructionDataDecoder(): FixedSizeDecoder<PoolInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]])
}

export function getPoolInstructionDataCodec(): FixedSizeCodec<
  PoolInstructionDataArgs,
  PoolInstructionData
> {
  return combineCodec(getPoolInstructionDataEncoder(), getPoolInstructionDataDecoder())
}

export type PoolAsyncInput<
  TAccountPool extends string = string,
  TAccountNullifierSet extends string = string,
  TAccountPoolVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool?: Address<TAccountPool>
  nullifierSet?: Address<TAccountNullifierSet>
  poolVault?: Address<TAccountPoolVault>
  authority: TransactionSigner<TAccountAuthority>
  systemProgram?: Address<TAccountSystemProgram>
}

export async function getPoolInstructionAsync<
  TAccountPool extends string,
  TAccountNullifierSet extends string,
  TAccountPoolVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PTRANS_PROGRAM_ADDRESS,
>(
  input: PoolAsyncInput<
    TAccountPool,
    TAccountNullifierSet,
    TAccountPoolVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountNullifierSet,
    TAccountPoolVault,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PTRANS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    nullifierSet: { value: input.nullifierSet ?? null, isWritable: true },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >

  // Resolve default values.
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({ programAddress })
  }
  if (!accounts.nullifierSet.value) {
    accounts.nullifierSet.value = await findNullifierSetPda(
      { pool: getAddressFromResolvedInstructionAccount('pool', accounts.pool.value) },
      { programAddress },
    )
  }
  if (!accounts.poolVault.value) {
    accounts.poolVault.value = await findPoolVaultPda(
      { pool: getAddressFromResolvedInstructionAccount('pool', accounts.pool.value) },
      { programAddress },
    )
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pool', accounts.pool),
      getAccountMeta('nullifierSet', accounts.nullifierSet),
      getAccountMeta('poolVault', accounts.poolVault),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as PoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountNullifierSet,
    TAccountPoolVault,
    TAccountAuthority,
    TAccountSystemProgram
  >)
}

export type PoolInput<
  TAccountPool extends string = string,
  TAccountNullifierSet extends string = string,
  TAccountPoolVault extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  pool: Address<TAccountPool>
  nullifierSet: Address<TAccountNullifierSet>
  poolVault: Address<TAccountPoolVault>
  authority: TransactionSigner<TAccountAuthority>
  systemProgram?: Address<TAccountSystemProgram>
}

export function getPoolInstruction<
  TAccountPool extends string,
  TAccountNullifierSet extends string,
  TAccountPoolVault extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PTRANS_PROGRAM_ADDRESS,
>(
  input: PoolInput<
    TAccountPool,
    TAccountNullifierSet,
    TAccountPoolVault,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): PoolInstruction<
  TProgramAddress,
  TAccountPool,
  TAccountNullifierSet,
  TAccountPoolVault,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PTRANS_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    pool: { value: input.pool ?? null, isWritable: true },
    nullifierSet: { value: input.nullifierSet ?? null, isWritable: true },
    poolVault: { value: input.poolVault ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId')
  return Object.freeze({
    accounts: [
      getAccountMeta('pool', accounts.pool),
      getAccountMeta('nullifierSet', accounts.nullifierSet),
      getAccountMeta('poolVault', accounts.poolVault),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getPoolInstructionDataEncoder().encode({}),
    programAddress,
  } as PoolInstruction<
    TProgramAddress,
    TAccountPool,
    TAccountNullifierSet,
    TAccountPoolVault,
    TAccountAuthority,
    TAccountSystemProgram
  >)
}

export type ParsedPoolInstruction<
  TProgram extends string = typeof PTRANS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    pool: TAccountMetas[0]
    nullifierSet: TAccountMetas[1]
    poolVault: TAccountMetas[2]
    authority: TAccountMetas[3]
    systemProgram: TAccountMetas[4]
  }
  data: PoolInstructionData
}

export function parsePoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    })
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pool: getNextAccount(),
      nullifierSet: getNextAccount(),
      poolVault: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getPoolInstructionDataDecoder().decode(instruction.data),
  }
}
